// Use this config file for global configuration

module.exports = {
  // Global server config
  server: {
    port: 8080,
  },

  // Electron app config
  electron: {
    width: 1600,
    height: 980,
  },
};
